name: stop id-cloudhost
on:
  schedule:
    - cron: "35 7 * * *"
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:

    - name: curl stop vm
      uses: enflo/curl-action@master
      with:
        curl: ${{ secrets.CURL_STOP }}
